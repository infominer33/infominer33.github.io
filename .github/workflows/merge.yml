name: Sync multiple branches
on:
  push:
    branches:
      - 'gh-pages'
jobs:
  sync-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Merge ghpages -> master
        uses: devmasx/merge-branch@master
        with:
          type: now
          from_branch: gh-pages
          target_branch: master
          github_token: ${{ github.token }}